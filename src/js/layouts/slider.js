var gl,
glide = {
	settings: {

	},
	init: function() {
		gl = this.settings;
		this.bindUIActions();
		console.log('glide loaded!');
	},
	bindUIActions: function() {
		// var glide = new Glide('.glide', {
		// 	type: 'carousel',
		// 	startAt: 0,
		// 	perView: 3
		// })
		// glide.mount();
	}
};
